[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scenes/enemies/minion.gd" id="1_minion"]

; NOTE: sprite_texture/sprite_scale are deliberately NOT set here. They used to
; be (the stage-1 minion art at 1.8), which meant Minion._configure's variant roll
; never ran on stage 1 — the second stage-1 variant (the big mage) was listed
; but could never appear (Designer, 2026-07-25: "bring back the big mage").
; Leaving both unset routes stage-1 minions through the same
; SPRITE_VARIANTS_BY_LEVEL / SPRITE_SCALE_BY_LEVEL path every other stage uses.
[node name="Minion" type="Node2D"]
script = ExtResource("1_minion")
max_hp = 20.0
damage = 3.0
attack_interval = 0.8
attack_range = 18.0
detect_range = 70.0
xp_value = 2
move_speed = 90.0
body_color = Color(0.753, 0.314, 0.302, 1)
body_radius = 11.0
bob_amplitude = 3.0
bob_speed = 6.0
