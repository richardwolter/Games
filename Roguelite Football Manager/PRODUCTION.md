# Roguelite Football Manager — Production

**Status:** Living Document (current state, updated as work progresses)

## Current Milestone

Milestone 18 in progress — Phases 1–4: Foundational State, Momentum, Tactical, Pressure, & Chance Generation/Resolution Systems, Event System. All systems wired and ready for verification (not yet tested in-engine).

## Completed Milestones

0. Project setup: doc structure (CLAUDE.md, Game_Design_Bible.md, PRODUCTION.md), base folder layout (`scenes/`, `scripts/`, `autoloads/`, `resources/`).
1. Club Creation flow (name, colors, procedural badge, persisted via GameState). Designer-approved 2026-07-17.
2. Squad generation (23-player roster, detailed positions, 5 core stats, off-position penalty hook). Designer-approved 2026-07-17.
3. Club Management + Pre-Match Setup — Designer-approved 2026-07-17.
   - `resources/formation.gd` (`Formation`), `resources/formation_library.gd` (`FormationLibrary`): 4 designer-approved shapes (High Defense 5-3-2, Solid Defense 4-4-2, Solid Attack 4-3-3, High Attack 3-5-2), slots as an ordered `SlotCategory` (GK/DEF/MID/FWD) array.
   - `resources/position_compatibility.gd` (`PositionCompatibility`): detailed-position → slot-category stat multiplier table (BALANCE.md), used only for match-fielding.
   - `autoloads/game_state.gd`: `formation`, `lineup` (index-aligned with `formation.slots`), `captain`, `lineup_changed` signal, and `set_formation` / `assign_to_slot` / `clear_slot` / `set_captain` / `get_bench()`.
   - `scenes/club_management/club_management.tscn`: hub screen (badge + club name, links to Squad Overview / Pre-Match Setup / New Club).
   - `scenes/pre_match_setup/pre_match_setup.tscn`: formation picker, drag-drop lineup, live compatibility multiplier + color cue per slot, captain picker, bench (uncapped — see Design Sign-off Needed).
3.5. Close Game + Save/Load: `resources/save_data.gd`, `autoloads/save_manager.gd` (single-slot save to `user://savegame.tres`), `scenes/main_menu/main_menu.tscn` (New/Load/Close Game, now the boot scene), `scenes/pause_overlay/pause_overlay.tscn` (autoload CanvasLayer, Menu/Esc → Resume/Save Game/Close Game, available on every screen). Designer-approved 2026-07-17.
4. Match Simulation Core Loop (score-only slice) — Designer-approved 2026-07-17.
   - `resources/match_result.gd` (`MatchResult`): home/away name + score.
   - `scripts/opponent_lineup_builder.gd` (`OpponentLineupBuilder`): generates a random opponent squad, formation, and greedily-filled lineup (reuses `SquadGenerator`/`FormationLibrary`/`PositionCompatibility`).
   - `scripts/match_simulator.gd` (`MatchSimulator`): team-strength + Poisson goal formula, first-pass placeholder — see [BALANCE.md](BALANCE.md) "Match Simulation".
   - `autoloads/game_state.gd`: added `last_match_result` (transient, not persisted in saves).
   - Pre-Match Setup: "Play Match" button (enabled only once all 11 lineup slots are filled) generates an opponent, runs the sim, and routes to `scenes/match_result/match_result.tscn` (score display + Continue back to Club Management).
   - Scope intentionally limited to score-only outcome — no live match view, in-match decisions, morale/traits/synergy/events (none of those systems exist yet); those are separate future milestones.
5. Live Match View — stamina + substitutions (text/event-feed presentation) — implemented, **awaiting Designer verification** (not yet run in-engine this session; no `godot` MCP tool available).
   - `resources/live_match_state.gd` (`LiveMatchState`): per-minute match engine — see BALANCE.md "Live Match Simulation". Replaces the old single-roll `MatchSimulator` (removed, folded its Poisson sampling in).
   - `autoloads/game_state.gd`: added `pending_opponent` (transient handoff from Pre-Match Setup to the live match scene, not persisted in saves).
   - `scenes/pre_match_setup/pre_match_setup.gd`: "Play Match" now generates the opponent and routes to `scenes/live_match/live_match.tscn` instead of simulating instantly.
   - `scenes/live_match/live_match.tscn`: text event feed (scrolling minute-stamped goal/sub log), live score/minute/subs-used labels, Play/Pause, Substitute (opens a starter/bench picker panel, capped at 5 subs, home side only), Continue → `match_result.tscn` (unchanged, now fed from the live match's final score instead of the old instant sim).
   - Explicitly out of scope this milestone (per Design sign-off below): animated/abstract pitch visualization, away-side (AI) substitutions, morale/traits/synergy/events, tactical commands, coach abilities.

6. Pre-Match Setup: Auto-Fill Empty Slots — implemented, **awaiting Designer verification** (no `godot` MCP connection this session).
   - `scenes/pre_match_setup/pre_match_setup.gd`: `_on_auto_fill_pressed` greedily assigns the best remaining bench player (highest `overall x eligibility multiplier`) to each currently-empty slot; slots already filled by the designer are never touched.
   - New "Auto-Fill Empty Slots" button in the ActionRow (`pre_match_setup.tscn`).
7. Live Match: overall + condition stats on the Substitute panel — implemented, **awaiting Designer verification**.
   - `scenes/live_match/live_match.gd` `_populate_sub_options`: starter dropdown now shows `OVR` and current `COND %`; bench dropdown shows `OVR`. No new resources/state — reuses `Player.get_overall()` and `LiveMatchState.get_condition()`.
8. Live Match: mid-match formation change — implemented, **awaiting Designer verification**.
   - `resources/live_match_state.gd`: `change_formation(new_formation, new_lineup)` — swaps `home_formation`/`home_lineup` in place; conditions are keyed by `Player` so they carry over untouched, no reset.
   - `scenes/live_match/live_match.gd` + `.tscn`: new "Change Formation" button opens a panel (formation picker + pitch slots + unplaced-starters pool, reusing `LineupSlot`/`PlayerChip`/`BenchDropArea` from Pre-Match Setup) that lets the designer manually re-place the **current 11 starters only** into the new shape. Bench players are not selectable here — by design, so this cannot be used to add fresh legs without spending a sub. Pauses ticking while open; resumes prior play/pause state on confirm or cancel.

9. Live Match + Match Result UI restyle to match Pre-Match Setup — Designer-approved 2026-07-17.
   - `scenes/live_match/live_match.tscn` + `.gd`: rebuilt into the same sidebar (title/score/minute/subs + action buttons) + main-area (panels) layout as Pre-Match Setup, replacing the old single stacked VBox. Margins (40/32), button heights (56px), and title font size (30) now match across screens.
   - `scenes/match_result/match_result.tscn`: margins aligned to 40/32 to match; layout otherwise unchanged (a centered score + Continue button doesn't need a sidebar split).

10. Live Match: queued multi-substitution + formation-change Auto-Fill Team — Designer-approved 2026-07-17.
    - `scenes/live_match/live_match.gd`: Substitute panel now queues multiple starter/bench pairs (numbered `#1`, `#2`, ...) before a single "Confirm All Subs" applies them all to `LiveMatchState`; clicking an already-queued player again un-queues it. "Cancel" now discards the queue instead of applying anything (previously each pick applied immediately). Cap of 5 subs enforced across already-used + queued picks.
    - `scenes/pre_match_setup/lineup_slot.gd` + `player_chip.gd`: added shared `queue_number` property (orange highlight + `#N` label) reused by the Substitute panel; unused by Pre-Match Setup itself, no behavior change there.
    - `scenes/live_match/live_match.gd` + `.tscn`: new "Auto-Fill Team" button on the Change Formation panel — greedily places unplaced starters into empty formation slots (same algorithm as Pre-Match Setup's auto-fill, scoped to the pending lineup). Confirm stays disabled until fully filled.

11. Match Visual Prototype: Pitch + Player Sprites (standalone test) — implemented, **awaiting Designer verification** (screenshotted via `godot-ai` this session; not yet reviewed by Designer live).
    - Source art: `Soccorpia Asset Pack.zip` (Designer-provided) — `assets/soccorpia/` now holds the ball, stadium tileset, and one Idle/Running/Victory-Kick sheet trio each for a home (`Soccer Player`) and away (`Competitor Soccer Player`) placeholder character.
    - `resources/tilesets/stadium_tileset.tres`: 16x16 `TileSetAtlasSource` over the full 74-tile stadium strip (stand/track/grass/line-marking tiles).
    - `scenes/pitch_prototype/pitch_prototype.tscn` + `.gd`: standalone scene (not wired into `scenes/live_match`) — a `TileMapLayer` used only for the stand/track border rows (top and bottom, per the tileset's own stand→track→pitch row order); the grass itself is a flat `ColorRect` (sampled from the tileset's grass tile color) rather than tiled, since tiling the flat-color grass tile at this scale produced visible per-tile seam lines that don't belong on a real pitch. A `Markings` Node2D (`pitch_markings.gd`) draw-calls the official boundary/halfway-line/center-circle/penalty-box lines over the grass — no unofficial markings. A `Goals` Node2D (`pitch_goals.gd`) draws a procedural goal frame + net hatch at each end (the asset pack has no goal sprite). Field proportions are ~1.5:1 (40x26 grass tile grid) to match a real pitch's ~105x68m ratio.
    - 11-a-side placeholder lineup on both sides, positioned by reusing the existing `Formation`/`FormationLibrary` resources (`resources/formation.gd`, `resources/formation_library.gd` — no new formation data invented): `_compute_formation_positions` places each `Formation.SlotCategory` row at a fixed depth-fraction from each team's own goal line, spread evenly across pitch width, mirrored per side. Each of the 22 `AnimatedSprite2D`s (`HomeTeam`/`AwayTeam` containers, children `P0`-`P10`) auto-cycles idle (2s) → run (1.5s) → kick (one-shot) → idle on an independent randomly-phased timer so the team doesn't animate in lockstep. `SpriteFrames` are sliced from the 100x103-frame sheets in code (`_build_sprite_frames`), not hand-authored resources.
    - Player display scale (1.2x native sheet content) is a hardcoded art-only constant in `pitch_prototype.gd`, deliberately not a `BALANCE.md` value (visual sizing, not a gameplay tunable) — chosen so characters read as "a little bigger" than the ball per Designer sign-off.
    - Explicitly out of scope (superseded by Milestone 12 below): wiring into the real match simulation / live match view, using each side's actual saved lineup/formation.

12. Live Match: animated pitch view wired in — Designer-approved 2026-07-17.
    - `scenes/live_match/live_match.tscn`: `MainArea` now holds `PitchPanel` (a `SubViewportContainer`/`SubViewport` embedding an instance of `scenes/pitch_prototype/pitch_prototype.tscn`, stretch ratio 3) above a shrunk `EventsPanel` (stretch ratio 1, min height 160) — per Designer's layout call, the text event feed becomes a ticker under the animated pitch rather than being replaced.
    - `scenes/pitch_prototype/pitch_prototype.gd`: reworked from the standalone-only prototype into a reusable view with a public API — `configure(home_formation, home_lineup, away_formation, away_lineup)`, `update_lineup(slot_index, incoming)`, `update_formation(new_formation, new_lineup)`, `play_kick(is_home, scorer)`. Standalone use (running `pitch_prototype.tscn` directly) still works unchanged — `_ready()` defaults both sides to a sample formation when `configure()` isn't called externally. Added `_update_camera_fit()` so the same scene auto-scales whether it fills the full window (standalone) or a smaller embedded `SubViewport` (Live Match), never zooming in past native scale.
    - `resources/live_match_state.gd`: added `goal_scored(is_home, scorer)`, `substitution_made(slot_index, incoming, outgoing)`, and `formation_changed` signals, emitted from the existing `_score_goal`/`substitute`/`change_formation` methods — purely additive plumbing so the UI/visual layer can react without parsing the `events` text log; no simulation numbers changed.
    - `scenes/live_match/live_match.gd`: on `_ready`, calls `pitch_view.configure(...)` with the real `LiveMatchState` formations/lineups (no more hardcoded formation) and connects the three new signals to `pitch_view.play_kick` / `update_lineup` / `update_formation`.
    - Since the asset pack has only one placeholder character per side, a substitution has no visual difference (same sprite) — `update_lineup` only updates the internal Player→slot mapping so a later goal by the sub resolves to the correct pitch position for the kick animation.
    - Verified in-engine this session via `godot-ai` `game_eval` (scripted a full club/squad/formation/lineup/opponent setup and jumped straight to Live Match, then advanced the full 90 minutes): formation-accurate pitch layout for both sides, goal event correctly played the scorer's kick animation and sent the ball to the attacked goal, sidebar/ticker/Continue flow all intact, no script errors (only pre-existing harmless WASAPI audio warnings).
    - Explicitly out of scope: away-side visual reactions beyond goals (away never subs/changes formation this milestone, per existing rule), per-player distinct art (still one placeholder character per side).
    - Fix (same session): `_update_camera_fit()` had the `Camera2D.zoom` direction backwards (screen_px = world_units * zoom — a higher zoom magnifies/zooms IN, it does not zoom out), so embedded-in-Live-Match the camera was zoomed in past the field's edges, clipping the goal frames off-screen entirely. Corrected to `zoom = min(vp_size / padded_field_size, 1.0)`; re-verified via `game_eval` that both goals are now visible with margin in the embedded SubViewport, and standalone (`pitch_prototype.tscn` run directly) is unaffected.

13. Live Match: simplified attribute-driven match engine — Designer-approved 2026-07-17.
    - New `resources/match_engine.gd` (`MatchEngine`, `class_name`): replaces the old flat per-minute Poisson-on-goals formula with a simplified attribute-contest engine, adapted from Football Manager's Match Engine concept per Designer's request — reuses only the existing 5 `Player` stats, in-match Condition, and `Formation`/`PositionCompatibility` data (no new attributes/traits invented, per Designer sign-off). See BALANCE.md "Live Match Simulation" for the full attack/defense-rating, chance-creation, and shot-resolution formulas.
    - `resources/live_match_state.gd`: `advance_minute()` now calls `MatchEngine.resolve_minute(...)` once per tick and applies each returned shot result via new `_apply_shot_result`; removed the superseded `_roll_goals`/`_score_goal`/`_pick_scorer`/`_team_strength`/`_sample_poisson`/`BASE_EXPECTED_GOALS`. Added `shot_attempt(is_home, shooter, outcome)` signal (outcome `"GOAL"/"SAVED"/"OFF_TARGET"`), emitted for every attempt; `goal_scored` still fires alongside it only on `"GOAL"`, keeping Milestone 12's wiring contract unchanged.
    - `scenes/pitch_prototype/pitch_prototype.gd`: factored `play_kick`'s tween/animation body into a private `_animate_shot`; added `play_shot_attempt(is_home, shooter)` reusing the same ball-flight/kick visual for near-misses (no new art — the pack has no distinct "save" animation, an accepted simplification).
    - `scenes/live_match/live_match.gd`: connects `_match.shot_attempt` to a new `_on_shot_attempt`, which calls `pitch_view.play_shot_attempt` only when `outcome != "GOAL"` (goals stay fully handled by the existing `goal_scored` → `play_kick` path, avoiding double-animating).
    - Verified in-engine via `godot-ai` `game_eval`: ran several full 90-minute matches confirming a believable mix of GOAL/SAVED/OFF_TARGET events (score variance traced to genuine squad-strength differences from random generation, not a formula asymmetry — checked attack/defense ratings directly for both sides), and confirmed the embedded Live Match view animates correctly through a full match with the existing sidebar/ticker/Continue flow intact (no regression, since `LiveMatchState`'s public surface didn't change shape).
    - Explicitly out of scope: tactical instructions (Next Candidate below, no values defined), player traits (Known TBD), full per-frame positional movement/pathfinding of all 22 sprites (still ambient idle/run/kick auto-cycle from Milestone 11 except during a shot event).

14. Live Match: frame-by-frame player movement + possession/ball physics — implemented same era as Milestone 13 but not written up at the time; backfilled here for the record. Added `scripts/player_movement_system.gd`, `scripts/possession_system.gd`, `resources/ball_state.gd`, `resources/player_position_state.gd` — real-time pitch positioning layered visually on top of `MatchEngine`'s per-minute results. Superseded by Milestone 17 below, which folds this layer and `MatchEngine` into one system (the old version had the visuals and the score decided independently of each other — see Milestone 17's Context for why that had to change).

15. Live Match: goal celebration overlay — implemented same era as Milestone 13, not written up at the time; backfilled here. `scripts/goal_celebration_system.gd` (`GoalCelebrationSystem`): a 2.5s celebration sequence on goal, players converge toward the goal briefly, then both sides reset to formation shape and the conceding side restarts from center. Simulation keeps running underneath (no hard pause). Unaffected by Milestone 17 — reused as-is.

16. Live Match: possession-duel engine (`MatchEngine` rewrite) — implemented same era as Milestone 13, not written up at the time; backfilled here. Replaced the original independent-Poisson-per-side model with discrete per-minute "duels" between whole-team attack/defense averages advancing an abstract `Zone` (MIDFIELD→ATTACKING_THIRD→BOX→BREAKAWAY). **Fully superseded by Milestone 17 below** — `MatchEngine` and this duel/zone model are deleted, not just modified.

17. Live Match: unified real-time match engine — implemented, **awaiting Designer verification** (verified via `godot-ai` this session; not yet reviewed live by Designer).

18. Live Match: momentum + team state + tactical + pressure systems (Phases 1–2 of unified narrative engine) — implemented, **awaiting Designer verification in-engine**.
    - **Context:** M17's engine is position-driven (individual shot/pass/dribble decisions), but design calls for momentum/pressure driving match narrative — higher-level team behavior flowing from events, not isolation. Phases 1–2 establish foundational state and tactical/pressure systems that later phases (chance generation, events) will build on.
    - **Phase 1 - Foundational State & Momentum:**
      - New `resources/team_match_state.gd` (`TeamMatchState`): per-team hidden state (momentum, possession %, territory %, confidence, organization, physical energy, aggression, tactical style, pass chain, recent events, pressure level).
      - New `scripts/momentum_system.gd` (`MomentumSystem`): applies momentum changes on events (goal ±35/30, save +8, tackle/interception +4/+5, pass chain +1.5 per pass, turnovers −8, cards −5/−20); decays naturally each periodic update; updates team characteristics (confidence, organization, physical energy) based on momentum and match state.
      - `resources/live_match_state.gd`: added team states, momentum system, `update_team_states(delta)` method (called every 3 real-time seconds from live_match.gd), momentum applied on goal/save events via record_shot_result, pass chains reset on turnovers.
      - `scenes/live_match/live_match.gd`: added periodic team-state update (every 3s, scaled by speed multiplier).
      - Possession tracking: each team accumulates possession_time_seconds while holding the ball; possession_pct recalculated each update as (time / total) × 100.
    - **Phase 2 - Tactical & Pressure Systems:**
      - New `scripts/tactical_system.gd` (`TacticalSystem`): defines the 5 tactical styles (High Press, Possession, Counter Attack, Park The Bus, Long Ball) with modifier tables for organization, aggression, confidence, possession %, pass completion, interception, tackle, shot chance, and fatigue. Each style modifies how pressure builds and how chances are generated.
      - New `scripts/pressure_system.gd` (`PressureSystem`): calculates continuous pressure (0–100) from weighted average of momentum (30%), possession (25%), territory (25%), and tactical base (20%). Territory calculated from fielded player positions (attacking players in opponent's half vs defending players in own half). Pressure thresholds spawn chances (40%→small, 60%→good, 80%→big, 90%→clear-cut) with xG values (first-pass: 0.05–0.95).
      - `resources/live_match_state.gd`: added `tactical_system`, `pressure_system`, `movement_system_ref` (for territory calculation), `set_movement_system()` method, tactical modifiers + pressure calculation called each periodic update.
      - `scenes/live_match/live_match.gd`: wired movement system reference to match state for territory calculation.
    - **Phase 3 - Chance Generation & Resolution:**
      - New `resources/chance.gd` (`Chance`): data object representing a live chance (chance_type, xG, striker, spawn_time, duration 10–30s, taken flag).
      - New `scripts/chance_generator.gd` (`ChanceGenerator`): spawns one chance per pressure threshold crossing (40%/60%/80%/90%) + bonus Big chances every 15s if pressure sustained >80% (Designer decision Option C). Pre-selects highest-rated forward/striker as shooter. Tracks active chances per team, expires when duration exceeded.
      - New `scripts/chance_resolver.gd` (`ChanceResolver`): resolves chance → GOAL/SAVED/OFF_TARGET via `goal_probability = xG × striker_quality / goalkeeper_defense` (condition-scaled Kick/Strength stats). SAVED vs OFF_TARGET split favors higher Kick strikers.
      - `resources/live_match_state.gd`: added `chance_generator`, `chance_resolver`, `match_time_seconds` (for expiry tracking). `update_team_states()` calls chance generator each update. New `_auto_resolve_expired_chances()` resolves chances whose duration exceeded.
      - `scenes/live_match/live_match.gd`: track match_time_seconds during periodic updates.
      - **Designer decisions (Phase 3):** Option C (threshold + sustained bonus), Option B (pre-select highest-rated striker), Option B (10–30s duration, auto-resolve on expiry; allows delay but expires if not taken). Manual chance trigger UI deferred to Phase 5.
    - **Phase 4 - Event System (Fouls, Cards, Injuries):** implemented, **awaiting Designer verification in-engine**.
      - New `scripts/event_system.gd` (`EventSystem`): tracks discipline per player, triggers fouls/cards on tackles based on aggression/pressure, injuries on high-intensity contacts.
      - Yellow card: first foul per player (−5 momentum); Red card: second foul (−20 momentum, ejection). Injury: −10 momentum.
      - `scripts/match_decision_engine.gd`: integrated tackle event checking after each tackle contest.
      - `resources/live_match_state.gd`: added `apply_match_event()` to log events and apply momentum penalties.
      - See PHASE_4_SUMMARY.md and BALANCE.md "Event System" for full details.
    - **Not yet implemented:** UI indicators (momentum bar, pressure gauge, chance highlights, Phase 5), player ejection/injury enforcement (Phase 5), commentary system (Phase 5). See PHASE_1_SUMMARY.md/PHASE_2_SUMMARY.md/PHASE_3_SUMMARY.md/PHASE_4_SUMMARY.md for phase breakdowns.
    - **Context:** Designer feedback — the live match "feels very odd and unnatural," wanting to actually watch passing, players positioning to receive the ball, ball disputes, and possession recovery unfold rather than glance at a result. Investigation found Milestones 13/14/16 had built two disconnected simulations: `MatchEngine` (invisible per-minute duel/zone dice roll) actually decided the score, while `PlayerMovementSystem`/`PossessionSystem`/the old `PassSystem` was a decorative layer picking its own passes via an independent per-minute RNG with no relationship to what `MatchEngine` did that minute — confirmed by reading both systems, they shared no resolution logic. Also found `pitch_prototype.gd`'s `animate_pass()` was a no-op stub (no visible ball-flight arc ever existed), and `PlayerMovementSystem` recomputed a fresh heuristic target every frame with no persistent steering, causing visible jitter. Designer sign-off: full replacement, not a patch — one system where real positions/stats are the only source of truth for both the visuals and the score, at real-time pacing with speed control instead of a ~31-second dice roll.
    - New `scripts/match_decision_engine.gd` (`MatchDecisionEngine`, `class_name`): single source of truth for shots/passes/tackles/interceptions, replacing `resources/match_engine.gd` and `scripts/pass_system.gd` (both deleted). Carrier decisions (shoot/pass/dribble), tackle contests, and pass-interception rolls all use real player positions (`PlayerMovementSystem.get_player_position`) and per-player (not team-average) condition-scaled stat contests. See BALANCE.md "Live Match Simulation" for every formula/constant.
    - `resources/ball_state.gd`: added pass-in-flight state (`pass_in_flight`, `pass_target_player`, `pass_start_pos`/`pass_end_pos`, `pass_elapsed`/`pass_duration`, `pass_success`, `pass_interceptor`) and a `start_pass()` entry point, so a pass is a real timed event with a resolved outcome instead of an instant carrier reassignment.
    - `scripts/possession_system.gd`: reworked to interpolate the ball along an in-flight pass each frame (`_update_pass_flight`) and resolve arrival (complete → new carrier, intercepted → loose ball); loose-ball pickup (`_check_loose_ball_pickup`) changed from "nearest player wins outright" to a Strength/Speed-weighted contest when 2+ players from both sides reach it at once — a genuine visible 50/50. Dropped the embedded `PassSystem` and the now-dead `_assign_possession_player` (no longer reachable now that `MatchEngine`'s team-only-possession sync hack is gone).
    - `scripts/player_movement_system.gd`: added acceleration-capped steering (`ACCELERATION`, `ARRIVAL_RADIUS` — velocity turns toward the desired heading instead of snapping) to remove the frame-to-frame jitter, and an explicit "receiving a pass" target branch (runs to meet the ball's real flight endpoint while a pass they're the intended receiver of is in the air). Added `home_attacks_right()` getter so `MatchDecisionEngine`'s goal-distance checks always agree with what's rendered.
    - `scenes/pitch_prototype/pitch_prototype.gd`: `animate_pass()` now does something (a passer kick-animation flourish) instead of being a no-op — the ball's own flight is driven every frame by the `BallState` interpolation above, already picked up by the existing `update_ball_state()`.
    - `resources/live_match_state.gd`: reworked into a reactive data holder — `advance_minute()` now only does condition decay and the full-time check (no more `MatchEngine.resolve_minute` call, no more duel/zone/possession-sync state); new `record_shot_result()` (renamed from `_apply_shot_result`) is called by `live_match.gd` whenever `MatchDecisionEngine` resolves a shot, keeping the existing `goal_scored`/`shot_attempt` signal contract (and therefore the pitch-view/celebration wiring) unchanged.
    - `scenes/live_match/live_match.gd` + `.tscn`: replaced the fixed 0.35s `TickTimer` with a real-time clock (`SIM_SECONDS_PER_SIM_MINUTE` = 3.3 — a full match takes ~5 real minutes at 1x) driven from `_process`; added 1x/2x/4x speed buttons that scale the `delta` fed to `MatchDecisionEngine`/`PossessionSystem`/`PlayerMovementSystem`/the clock uniformly. Added `_kickoff()` (home kicks off the match, the side that didn't kick off a half takes the next one, reused for post-goal restarts too — previously only post-goal restarts had explicit kickoff-taker logic).
    - Verified in-engine via `godot-ai`: filesystem `scan`/`reimport` needed after adding the new `class_name MatchDecisionEngine` script (new global classes aren't picked up until a scan). Ran full synthetic matches by manually stepping `_process()` in fixed 0.05s increments (avoids a real-time-await/window-focus quirk in the `game_eval` tool that otherwise stalled wall-clock timers) — confirmed goals/passes/tackles/interceptions all fire from genuine position-driven contests (79 carrier changes + 31 completed pass flights over one 150-simulated-second sample), half-time pause/resume and second-half kickoff work, a full 90-minute match reaches full time cleanly, and substitution/formation-change panels still pause/resume play correctly with the new `_is_playing`-flag-based pacing. No script errors in any run. Fixed two new unused-variable warnings surfaced during verification (an `on_half_time_side_switch` parameter shadowing the new `home_attacks_right()` getter; an unused `opp_formation` parameter on a helper).
    - Explicitly out of scope: tactical instructions, player traits, morale/synergy/events (none of those systems exist yet); away-side substitutions; fouls/cards/offside.

## Demo Scope

TBD — full scope not yet defined beyond Milestone 1. To be expanded as milestones complete.

## UI/Presentation

- Game runs in exclusive fullscreen at a 1920x1080 base resolution (`canvas_items`/`expand` stretch). Global theme (`theme/main_theme.tres`) sets default font size 22 across controls. Verified in-engine 2026-07-17 — all 4 scenes (Club Creation, Club Management, Squad Overview, Pre-Match Setup) confirmed working by Designer.
- Squad Overview's stat table columns were widened (`squad_overview.gd` `COLUMN_WIDTHS`) to use the full window width.
- Not yet resized: Pre-Match Setup's procedurally-built pitch slots and bench cards (`pre_match_setup.gd`) — still using their original sizing.

## Next Candidates

- Tactical style selection (Game_Design_Bible.md:36) — no values/effects defined yet
- Away-side (AI) substitutions during the live match

## Design Sign-off Needed

- Tactical style list and mechanical effects (deferred out of Milestone 3)
- Abstract pitch view with moving markers for the live match (deferred out of Milestone 5 — current milestone ships a text event feed only; pitch visuals need their own milestone once the sim engine is verified)
- Live match condition/decision/tackle/interception/shot formulas (BALANCE.md "Live Match Simulation", rewritten for Milestone 17's `MatchDecisionEngine`) are first-pass placeholders, not yet Designer-verified in-engine for actual match feel/balance
- Real-time match pacing (`SIM_SECONDS_PER_SIM_MINUTE` = 3.3, ~5 real minutes per match at 1x) and the 1x/2x/4x speed options are first-pass placeholders — not yet Designer-verified for whether that's the right length for a roguelite played many matches per run

## Production Notes

- Engine: Godot 4.7-stable
- Project path: `C:\Users\richa\Documents\Roguelite Football Manager`
- GDD source: `C:\Users\richa\Desktop\Game Idea\Roguelite Football Manager\GDD - Roguelite Football Manager.txt`
- Tools: `godot` MCP connected for automation (invoked with this project's path)
